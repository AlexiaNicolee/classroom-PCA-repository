#include <stdio.h>
#include <stdlib.h>

    void main()
{
    float raio, area ;
    printf("\n Area de um circulo");
    printf("\n Entre com o raio :");
    scanf ("%f",&raio );

    area = ((raio*raio)*3.1415);
    printf("\n A area do circulo eh %f ", area);
    // Calcular a area do circulo

    return 0;
}
